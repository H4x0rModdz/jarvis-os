#!/bin/sh
# Play/pause whichever MPRIS-aware media player is currently focused.
# playerctl picks the most recently active player automatically; no
# config needed for the typical case (one player at a time).
set -eu

playerctl play-pause 2>/dev/null || exit 0
